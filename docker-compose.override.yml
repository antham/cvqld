version: '2'
services:
  server:
    build:
      context: .
      dockerfile: Dockerfile-public
    ports:
      - "80:80"
  app:
    build: .
    env_file: .env
    restart: always
    command: askme_linux_amd64
  postgres:
    image: postgres
    ports:
      - "5432"
